spring:
  application:
    name: fee-payment
  mvc:
    problemdetails:
      enabled=true:

  datasource:
    url: jdbc:mysql://localhost:3306/student_mgt_db?serverTimezone=GMT
    username: root
    password: Summer123!

  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
  sql:
    init:
      mode: always
  flyway:
    enabled: true

server:
  port: 8000

#logging:
#  level:
#    org:
#      springframework: DEBUG

